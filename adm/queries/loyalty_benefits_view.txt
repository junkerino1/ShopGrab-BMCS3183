CREATE OR REPLACE VIEW vw_loyalty_benefits AS
SELECT
    b.benefit_id,
    b.tier_id,
    t.tier_name,
    t.min_total_spend,
    b.benefit_type,
    b.description
FROM L_BENEFIT b
JOIN LOYALTY_TIER t ON t.tier_id = b.tier_id;
